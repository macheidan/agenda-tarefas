# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment variables
.env
.env.local
.env.*.local

# Credenciais do deploy FTP (senha) — NUNCA commitar
.env.ftp

# Firebase service account (script de importação de folha) — NUNCA commitar
serviceAccount*.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.vercel
scripts/motoboys/data/

# Coleta de clientes (nome + telefone de cliente) — dado pessoal, NUNCA commitar
scripts/clientes/data/

# Cache do Python (o coletor de clientes roda daqui)
__pycache__/
*.pyc

# Apps Scripts da Gestao (contem tokens de sync; colar na planilha, nao commitar)
scripts/gestao/*.gs

# Coletor do Dash (scripts/dash): senhas FTP/IMAP, token OAuth e dados com faturamento
scripts/dash/config.json
scripts/dash/*.pickle
scripts/dash/data/
